import { Box } from "@mui/material";

export default function Error(props) {
  return (
    <Box
      sx={{
        width: "100%",
        display: "flex",
        height: "100%",
        justifyContent: "center",
      }}
    >
        Page Not Found 404
    </Box>
  );
}
